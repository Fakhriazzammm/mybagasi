{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "installCommand": "npm cache clean --force && npm install --registry=https://registry.npmjs.org",
  "buildCommand": "npx vite build"
}
